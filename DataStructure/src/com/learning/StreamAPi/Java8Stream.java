package com.learning.StreamAPi;

import java.util.Arrays;
import java.util.Comparator;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.stream.Collectors;

public class Java8Stream {

    public static void main(String []args){
    /*    List<Employee> listofEmployee= Arrays.asList(new Employee("1","shashank",33),
                new Employee("2","shekhar",34),
                new Employee("3","sinha",36));


        listofEmployee.stream().sorted(ComparatorTest.comparing(Employee::getEmployeeAge));


        listofEmployee.stream().filter(e->e.getEmployeeName().equals("shashank")).map(User::new).
                forEach(System.out::println);

        System.out.println(listofEmployee.stream().mapToInt((e)->e.getEmployeeAge()).max());

        listofEmployee.stream().mapToDouble(e->e.getEmployeeAge());*/





    }
}
